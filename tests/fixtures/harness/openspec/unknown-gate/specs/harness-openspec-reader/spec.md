# harness-openspec-reader Specification

## ADDED Requirements

### Requirement: Loads change folders
The system SHALL return a task for valid folders.

#### Scenario: Valid folder
Given a valid folder
When it is parsed
Then a task is returned.
